# Initilization of objects
* What do different methods of initialization actually do?
    * `T t;`
    * `T t{};`
    * `T t{...};`
* What's the impact of a user-defined constructor and the default constructor?
* What's the impact of the member definition in the class definition?

# Output

```
Trivial class with default constructor:

tc_1;				-> garbage values
	a = 28941
	b = -3.78639e-08
	c = char(13)
tc_2{};				-> zero values, default constructor initializes to zero
	a = 0
	b = 0
	c = char(0)
tc_3{1, 1, '1'};		-> user defined values, constructor generated by compile which accepts values
	a = 1
	b = 1
	c = char(49)
tc_4{5};			-> constructor generated by compiler to fill values as defined by user or zero values
	a = 5
	b = 0
	c = char(0)

///////////////////////////////////////////////////////

Trivial class (containing an object of another trivial class) with default constructor:

tc2_1;				-> garbage values
	a = 0
	b = 0
	c = char(0)
	d.a = 0
	d.b = -3.78583e-08
	d.c = char(13)
tc2_2{};			-> zero values, default constructor initializes to zero
	a = 0
	b = 0
	c = char(0)
	d.a = 0
	d.b = 0
	d.c = char(0)
tc2_3{1, 1, '1', 2, 2, '2'};	-> constructor generated by compiler to fill values as defined by user
	a = 1
	b = 1
	c = char(49)
	d.a = 2
	d.b = 2
	d.c = char(50)

///////////////////////////////////////////////////////

Trivial class with a user defined empty constructor:

tcn_1;				-> garbage values
	a = -2147483648
	b = 0
	c = char(0)
tcn_2{};			-> garbage values, user defined constructor doesn't do anything
	a = 0
	b = 0
	c = char(0)
tcn_3{1, 1, '1'};		-> does not compile since user only defined constructor without any arguments. Compiler doesn't generate any other constructor once user defined one.

///////////////////////////////////////////////////////

Trivial class with default constructor and members zero initialized

tc3_1;				-> zero values, since the trivial members of the class are zero initialized (via "memberA{};")
	a = 0
	b = 0
	c = char(0)

///////////////////////////////////////////////////////

Class containing an array and default constructor

cc_1;				-> garbage values in array
	28941
	-1290672150
	28941
cc_2{};				-> zero values in int array
	0
	0
	0
cc_3{1, 1};			-> values are provided to array and first elements are filled with provided values, rest is zero
	1
	1
	0
```